{
    "$schema": "http://json-schema.org/draft-04/hyper-schema#",
    "title": "{{ title }}",
    "type": "object",
    "properties": {
        "id": {
            "type": "string",
            "links": [
                {
                    "rel": "full",
                    "href": "/{{ type }}/{$}"
                }
            ]
        }
    },
    "required": ["id"],
    "links": [
        {
            "rel": "delete",
            "method": "DELETE",
            "href": "/{{ type }}/{id}"
        },
        {
            "rel": "put",
            "method": "PUT",
            "href": "/{{ type }}/{id}"
        }
    ]
}
